<?php

/**
 * Implements hook_install().
 */
function samhsa_term_elevation_install($is_syncing) {
  if (PHP_SAPI == 'cli') {
    drupal_flush_all_caches();
  }
}

/**
 * Implements hook_uninstall().
 */
function samhsa_term_elevation_uninstall($is_syncing) {
  //
}